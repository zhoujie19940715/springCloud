eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/ #在工程的配置文件指定服务的注册中心地址
server:
  port: 9000
spring:
  application:
    name: ribbon-consumer-hystrix
#暴露全部的监控信息
management:
  endpoints:
    web:
      exposure:
        include: "*"